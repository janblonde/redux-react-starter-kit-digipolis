import React from 'react';

const login = prop => (
    <div>
		  <h2 className="h4 u-margin-top">Login</h2>
      <a href="/auth/login/mprofiel" className="a-button u-margin-top">Login with M-profile</a>
    </div>
);

export default login;
